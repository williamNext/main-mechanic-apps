## Agent skills

### Issue tracker

Local markdown files under `.scratch/<feature>/` (no git remote configured for this repo). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
