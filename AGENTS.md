## Agent skills

### Issue tracker

GitHub Issues via `gh` CLI (williamNext/oficina). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
