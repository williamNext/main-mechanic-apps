#!/usr/bin/env sh
set -eu

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks not found. Install gitleaks to enforce secret scan."
  exit 1
fi

gitleaks protect --staged --redact --verbose

